package gendev.bot.runner.aspectj;

/**
 * 
 * Task 1: Make the forklift beep before it goes backwards.
 * 
 * @author ringert
 *
 */
public privileged aspect BackBeep {
	
	pointcut backing(): call(void JavaAspectExecutor.backing());
	
	before() : backing() {
		((JavaAspectExecutor)thisJoinPoint.getTarget()).gui.beep();
	}

}
